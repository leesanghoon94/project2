# Use root/example as user/password credentials
version: '3.1'

services:
  server:
    image: qs2:1.0.0
    restart: always
    container_name: server
    ports:
      - "3000:3000"

  mongo:
    image: mongo
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: qook
      MONGO_INITDB_ROOT_PASSWORD: 1234